/.lake
# Default output of `lake exe vbp build` (verso-blueprint).
/_out/
# docs/ is no longer a separate Lake workspace; this only keeps pre-existing
# local docs/.lake trees (obsolete, safe to delete) out of `git status`.
docs/.lake/
docs/_out/

# Local runs of scripts/blueprint_progress_history.py (deploy artifact, not source)
docs/blueprint-progress-history.json

# Python bytecode caches (scripts/, docs/formalization-progress/)
__pycache__/
