# μCMZ, what remains to state

Branch `microcmz-remaining-statements`, worktree `KVAC-prj/kvac-statements`, from `main` at c5d44ca, reviewed against `main` at 626647d (10 September 2026). Every `sorry` on `main` appears in this list (items 5 and 12). Every blueprint node without a `lean :=` anchor appears in this list, named in parentheses. The node list is regenerated by `git grep -nE ':::(definition|theorem)' 626647d -- 'docs/KVACDocs/*.lean' | grep -v 'lean :='`. Completeness against the paper is read off the blueprint, the single source of truth of #103, which VeriLib renders. Every node tagged `paper, O24 …` that is unanchored, or anchors a declaration with a `sorry` (`single_attribute_mac`, `kvac_extractability`), appears in this list. Paper elements without a node yet are named in their items so that nodes can be added, Definition 9.1, Theorem 9.2, Remark 5.9, Equations 15 and 17 to 23, Figure 7, and the statistical anonymity corollary after Theorem 5.2.

**Shared prerequisites**

1. μCMZ `KVACSyntax` instance, `Credential.lean` (#163, part 4 of #6, node `mucmz_construction`), over abstract `NIZKPSyntax` parameters for R_iu, R_is, R_p, with its correctness `CorrectRO` (Definition 4.3) under completeness of the three parameters.
2. Partial‑disclosure predicate family {φ_a⃗} (#104), and the `Enforces` proofs of `riuSigma` and `rpSigma` for the family, which `Relations.lean` supplies only for `trivialPolicy`.
3. Hypothesis "ZKP proves R ⊇ R_cmz" with a composition clause for the shared `ZKRO H` (PR #139 stub), over the §3.3 argument properties (node `zk_arguments`).
4. Fiat–Shamir transform of the three Σ‑protocols (#3, rescope pending), oracle reprogramming `HashSpec.programFresh` (#60), the no‑coins lemma for the Fiat–Shamir verifier (#101), the encoding and domain‑separation obligations of `Core/Hash.lean`, and Theorem 9.2 (strong simulation extractability of Σ for the admissible relations of Definition 9.1, in the AGM and ROM). Deferrable, the instance is stated over abstract parameters and the bounds over abstract Adv^zk and Adv^ksnd terms. Item 11 needs the concrete Σ instantiation.

**Theorem 5.1, UF‑CMVA (Track CMZ‑M)**

5. Proof of `agm_ufcmva_le_n1_explicit` (Lemma 5.4, stated, `sorry`). Sub‑lemma skeleton in PRs #156 to #158, #161 and #162. The remaining sub‑lemmas are issues #108 to #116 (nodes `verify_help_oracle_coupling`, `verification_polynomial_consistency`, `run_level_coupling`, `sz_adaptive_bound`, `lem54_bound_assembly`, and `transcript_invariants`, `embedded_consistency_bricks` until #156 to #158 and #161 merge). Equation 15, the Help query representation, is the Help arm of #108.
6. Lemma 5.5, the n to 1 reduction (node `attribute_lifting`), with Claims 5.6 (collision branch, gap DL, node `forgery_case_gap_dl`) and 5.7 (forgery branch, node `forgery_case_mac`), and the general‑n bound `agm_ufcmva_le` with its gap DL summand (#80).
7. Bridge from the AGM game with Help to plain `UF_CMVAGame`, or the decision to keep the AGM game as the target (#81).
8. Theorem 5.1 statement (node `mucmz_mac_security`), with the target bound Adv^3‑dl + Adv^gap‑dl + 5/p of item 6 of `docs/presentations/rolf-status/errata.md`, replacing the printed Adv^3‑dl + Adv^dl + 3/p that the node text still shows.

**Theorem 5.8, anonymity (Track CMZ‑A)**

9. Anonymity game, Definition 4.4, `Framework/Anonymity.lean` (node `kvac_anonymity`).
10. Issuance and presentation simulators of §5.4, and the Theorem 5.8 bound (node `mucmz_anonymity`, not yet stated). PR #139 has a qualitative `Anonymous` scaffold under review, its three stubs `sorry` bodied since 8ddfa6b.
11. The corollary after Theorem 5.2, statistical anonymity of μCMZ[ZKP = Σ] under the hypothesis that Σ is statistically knowledge sound. Needs the Σ instantiation of item 4 and the bound of item 10.

**Theorem 5.10, extractability (Track CMZ‑E)**

12. `extractablePoly_obligation` (`Framework/Extractability.lean`, stated, `sorry`). As stated it asserts `ExtractablePoly` for every scheme and every efficiency predicate with no premises, so it needs a statement correction before a proof, either the premises the docstring elides (scheme correctness, knowledge‑sound ZKP with straight‑line extractors, and the MAC security and zero‑knowledge terms the bound carries) or a specialization to μCMZ.
13. The eight steps of the Theorem 5.10 plan. (1) Extractors and candidate‑Z recovery, (2) Help check, (3) identity lemma, (4) the reduction to UF‑CMVA, `extToUF`, (5) the single‑user restriction and partition, `NoMultiUser`, (6) the four bound terms (one zero‑knowledge hybrid and three terminal events, as in `docs/presentations/mucmz-security-plan/`) with the q_I and q_P + 1 factors, (7) the Theorem 5.10 inequality (node `mucmz_extractability`), and (8) the multi‑user extension lemma.
14. `newUsr_mac_isSome` (PR #140), its transport lemma `runRO_support_of_le` (stated, `sorry`, tracking issue to open under #118), and the cache growth invariant of `EXTGame` its application needs, that the shared cache at every `NewUsr` call extends the keygen cache.
15. The corollary that Definition 4.5 extractability implies the CMZ14 single‑user unforgeability of Figure 7, promised by `docs/PLAN.md` and the `framework_extract` blueprint text, in neither the code nor the blueprint nodes.

**Theorem 5.2**

16. Conjunction of 1, 9 and 10, and 12 to 14 at blueprint node `mucmz_extractable_kvac`.

**Theorem 5.3 and 5.11, anonymous token (Track CMZ‑OMUF)**

17. AGM‑instrumented OMUF game (step A4 of #12), Claims 5.12 to 5.14 (nodes `omuf_case_i`, `omuf_case_ii`, `omuf_case_iii`), the proof pieces A5 to A9 of #12 (representation bookkeeping, simulation, bad event bound, assembly) over the polynomial layer of Equations 17 to 22, the Theorem 5.11 bound at n = 1 (Equation 23), its extension to n > 1 (node `mucmz_at_agm_omuf`, with the constant audit the node text requires), the bridge to the plain Figure 6 OMUF game (on the pattern of #81), and the π_is lifting lemma (errata item 7).
18. The Figure 9 variant μCMZ_AT that keeps π_is, its syntax and correctness under completeness of π_is (the delivered core `μCMZATCore` of #165 removes both issuance proofs), and its game semantics, since `OMUFGame` takes `ATSyntax ProbComp` while a proof‑carrying variant lives over `ZKRO H`.
19. Theorem 5.3 (node `mucmz_at_omuf`) as the conjunction of correctness (18), the 5.8 simulator on the token variant, and Theorem 5.11 with the lifting lemma.

**Theorem 1**

20. Theorem 1 (node `mucmz_is_kvac`), which the blueprint node records as the conjunction of Theorems 5.2 and 5.3. Its printed bound is not the sum of the 5.2 and 5.3 bounds.

**Decisions blocking statements.** Extractor interface and Z recovery (13). Composition clause (3). AGM versus plain MAC game (7). Knowledge soundness or simulation extractability (Remark 5.9, items 3 and 13). Query factors or multi‑instance notion (13). Printed Theorem 5.3 and Theorem 1 bounds or the proof‑derived shapes only (19, 20, the π_is erratum is with the author). How `CorrectRO` reaches game states, the cache extension bridge of #118 (1, 14). Rescope of #3 (4). Concrete inequalities at fixed F and G or asymptotic negligibility over a λ‑indexed family (#148, items 12 and 16).
