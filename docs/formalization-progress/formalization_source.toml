# Source of truth for scripts/formalization_progress.py.
# Generated by `formalization_progress.py init`; review and edit by hand
# if needed (in particular the `tag`, which must match the citation
# prefix actually used in the Lean docstrings).

[source]
tag = "O24"
pdf = "docs/Orru_2024.pdf"
title = "Michele Orrù, *Revisiting Keyed-Verification Anonymous Credentials*, IACR ePrint 2024/1552"
lean_root = "KVAC"
exclude_dirs = ["Scratch"]
out_md = "docs/formalization-progress/FORMALIZATION_PROGRESS.md"
out_json = "docs/formalization-progress/formalization_progress.json"
summaries = "docs/formalization-progress/element_summaries.toml"  # curated one-line element summaries
